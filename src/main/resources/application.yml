server:
  port: 1221

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: ''
  jpa:
    open-in-view: true
    show-sql: true
    hibernate:
      ddl-auto: update
